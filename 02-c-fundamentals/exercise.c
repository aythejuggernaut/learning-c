#include <stdio.h>

int main(void) {
   int height, length, width;
   float decim, point;

   printf("Height - %d, Length - %d, Width - %d\n", height, length, width);
   printf("Decimal - %f, Point - %f", decim, point);
}

