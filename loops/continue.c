#include <stdio.h>

int main(void) {
    // break transfers control just past the end of a loop, while 
    // continue transfers control to a point just before the end of the loop
    // body. With break, control leaves the loop; with continue, control 
    // remains inside the loop. There's another difference between break and 
    // continue: break can be used in switch statements and loops (while, do, 
    // and for), whereas continue is limited to loops.
}